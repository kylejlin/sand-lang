pub class RangeCheckExpression {
    foo() {
        [
            i() in low() ... high(),
            i() in low() ..= high(),
            i() in low() =.. high(),
            i() in low() =.= high(),
        ]
    }
}