pub class BlockStatement {
    method() {
        {

        }

        {
            use foo.foo;
            use bar.bar;
        }

        {
            a = b;
        }

        {
            use foo.foo;
            use bar.bar;

            a = b;
        }
    }
}