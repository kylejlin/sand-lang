use foo.bar.Baz;
use foo.bar.Baz as B;
use! quz.baz as Baz;

pub class UseStatements {
    use! qux as foo;
    use foo.bar.qux;
    use foo.bar.qux as q;

    prop: int;

    method() {
        use foo.bar.yob;

        if yob {
            use q as z;
            use foo.yob;
            use! foo.bar.Baz as yob;

            z
        } else {
            qux
        }
    }
}