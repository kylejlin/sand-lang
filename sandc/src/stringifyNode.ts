import { Node } from "./types/ast";

export default function stringifyNode(node: Node): string {
  throw new Error(
    "TODO Implement stringifyAst(). This might indicate that a failure parser test case parsed successfully.",
  );
}
